---
server:
  port: 8081

spring:
  application:
    name: hibernate-OneToOne
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/OneToOneDB
    username: root
    password: Ib2019password
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
